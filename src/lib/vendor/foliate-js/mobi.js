// FumetoReaderPlus stub — NOT the upstream foliate-js module.
// The reflowable-EPUB subset never reaches this path (see PROVENANCE.md);
// the stub exists so the bundler can resolve view.js literal dynamic
// imports. Reaching it is a bug and fails loudly.
throw new Error("foliate-js 'mobi' is not vendored (reflowable subset)");
